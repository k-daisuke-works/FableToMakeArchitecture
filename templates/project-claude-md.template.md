# {{プロジェクト名}}

{{プロジェクトの一言説明}}

## 技術スタック

{{言語・フレームワーク・主要ライブラリ。未確定の項目は「TODO: 決まり次第記入」と書く}}

## コマンド

```bash
{{ビルド}}   # 例: npm run build
{{テスト}}   # 例: npm run test
{{リント}}   # 例: npm run lint
```

## 開発ルール

{{コードから読み取れない規約だけを箇条書き。例:}}
- {{テストが通るまでタスクを完了としない}}
- {{APIレスポンスの型は src/types/ に集約する}}

## スキルとエージェントの使い分け

{{存在するスキル・エージェントごとに1行。例:}}
- 新機能の実装を始めるときは `/new-feature` を使う
- 実装が終わったら `code-reviewer` エージェントに差分レビューを依頼する

## やってはいけないこと

- {{例: .env を読まない、本番DBに接続しない、main に直接 push しない}}
